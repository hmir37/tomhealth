"use client";

export function ComparisonWorkspace() {
  return (
    <div className="rounded-lg border border-[var(--color-border)] p-8 text-center text-[var(--color-text-muted)]">
      <p>
        Comparison workspace — select datasets and regions to compare. This
        component will include overlay charts, small multiples, and a ranked
        summary table.
      </p>
    </div>
  );
}
